{% import "_ads.njk" as ad %}{% import "_sponsor.njk" as sp %}{% import "_banner.njk" as bn %}<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  {% include "_seo.njk" %}

  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="assets/favicon.svg">
  <link rel="apple-touch-icon" href="assets/favicon.svg">

  <!-- Fonts: Open Sans Condensed (headlines) + Lato (UI) -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css?family=Lato:300,300italic,400,400italic,700,700italic,900|Open+Sans+Condensed:300,700&display=swap" rel="stylesheet">

  <!-- Styles -->
  <link rel="stylesheet" href="assets/css/tokens.css">
  <link rel="stylesheet" href="assets/css/base.css">
  <link rel="stylesheet" href="assets/css/layout.css">
  <link rel="stylesheet" href="assets/css/components.css">
  <link rel="stylesheet" href="assets/css/animations.css">
  <link rel="stylesheet" href="assets/css/pages.css">
  <link rel="stylesheet" href="assets/css/mobile.css">
</head>
<body>

  <div data-partial="loader"></div>
  <div data-partial="header"></div>

  <main>

    <!-- ============================================================
         HERO — cinematic full-screen
         ============================================================ -->
    <section class="hero" aria-label="Hero">
      <div class="hero-bg" data-parallax="-0.08">
        <img src="{{ home.hero.bgImage }}" alt="" loading="eager" fetchpriority="high" srcset="{{ home.hero.bgImage | srcset }}" sizes="100vw" decoding="async">
      </div>

      <div class="hero-inner hero-centered">
        <div class="hero-content">
          <h1>{{ home.hero.titleHtml | safe }}</h1>
          <p class="lead">{{ home.hero.lead | safe }}</p>
        </div>

        <!-- Search bar — centered inside hero. Runs the same keyword search as
             the header (FR-SEARCH): submits to /search?q=… -->
        <form class="hero-search hero-search-centered" method="get" action="/search" role="search">
          <div class="seg">
            <input class="seg-val" type="search" placeholder="Search destinations, packages, blog…" name="q" aria-label="Search">
          </div>
          <button class="btn btn-brand hero-search-btn" type="submit">Explore</button>
        </form>
      </div>

    </section>

    {{ bn.zone(banners, 'home-top') }}


    <!-- ============================================================
         FEATURED LOCATIONS (FR-HOME-005..007)
         ============================================================ -->
    <section class="section">
      <div class="container-wide">
        <div class="section-head" data-reveal>
          <div>
            <p class="overline">{{ home.featuredHead.overline }}</p>
            <h2>{{ home.featuredHead.titleHtml | safe }}</h2>
          </div>
          <div>
            <p>{{ home.featuredHead.blurb | safe }}</p>
            <a href="/categories" class="btn-text">View all destinations <span class="arrow">→</span></a>
          </div>
        </div>

        <div class="featured-grid" data-stagger>
{% for d in destinations %}
          <article class="feat-card">
            <a class="feat-media" href="/destination-detail?d={{ d.slug }}" aria-label="{{ d.name | striptags }} — view details">
              <img loading="lazy" src="{{ d.image }}" alt="{{ d.name | striptags }}" srcset="{{ d.image | srcset }}" sizes="(max-width: 640px) 92vw, (max-width: 1024px) 46vw, 30vw" decoding="async">
            </a>
            <div class="feat-info">
              <div class="region">{{ d.region }}</div>
              <h3 class="name">{{ d.name | safe }}</h3>
              <div class="meta">
                <span>{{ d.season }}</span><span class="dot"></span><span>{{ d.tagline }}</span>
              </div>
              <div class="feat-actions">
                <a class="act-details" href="/destination-detail?d={{ d.slug }}">Know More <span>→</span></a>
                <a class="act-pkg" href="/packages?d={{ d.slug }}">Check Packages <span>→</span></a>
              </div>
            </div>
          </article>
          {{ sp.banner(sponsorSlots[loop.index0], "sp-span-all") }}
          {{ ad.unit(adSlots[loop.index0], ads, "ad-span-all") }}
{% endfor %}
        </div>
      </div>
    </section>


    <!-- ============================================================
         TRAVEL HIGHLIGHTS / EXPERIENCES (FR-HOME-008..009)
         ============================================================ -->
    <section class="section-sm">
      <div class="container-wide">
        <div class="section-head" style="margin-bottom: var(--s-5);" data-reveal>
          <div>
            <p class="overline">{{ home.highlightsHead.overline }}</p>
            <h2>{{ home.highlightsHead.titleHtml | safe }}</h2>
          </div>
          <a href="/destinations" class="btn-text">All experiences <span class="arrow">→</span></a>
        </div>

        <div class="float-grid" data-reveal>
{% for h in highlights %}
          <a href="{{ h.href or '/destinations' }}" class="float-card">
            <img loading="lazy" src="{{ h.image }}" alt="{{ h.title }}, {{ h.place }}" srcset="{{ h.image | srcset }}" sizes="(max-width: 640px) 92vw, (max-width: 1024px) 46vw, 30vw" decoding="async">
            <div class="float-card-label">
              <span class="day">{{ h.kicker }}</span>
              <h3>{{ h.title }}</h3>
              <span class="place">{{ h.place }}</span>
            </div>
          </a>
{% endfor %}
        </div>
      </div>
    </section>




    <!-- ============================================================
         ANNOUNCEMENTS (FR-HOME-012..013D)
         ============================================================ -->
    <section class="section">
      <div class="container-wide">
        <div class="section-head" data-reveal>
          <div>
            <p class="overline">{{ home.announceHead.overline }}</p>
            <h2>{{ home.announceHead.titleHtml | safe }}</h2>
          </div>
          <a href="/announcements" class="btn-text">All announcements <span class="arrow">→</span></a>
        </div>

        <div class="announce-grid" data-stagger>
{% for a in announcements %}
          <article class="ann-card">
            <div class="ann-head">
              <span class="tag{% if a.tagClass %} {{ a.tagClass }}{% endif %}">{{ a.tagLabel }}</span>
              <span class="ann-date">{{ a.date }}</span>
            </div>
            <h3 class="h4-like">{{ a.title | safe }}</h3>
            <p class="ann-excerpt">{{ a.excerpt | safe }}</p>
            <a class="ann-link" href="{{ a.href }}">{{ a.linkLabel }} <span class="arrow">→</span></a>
          </article>
{% endfor %}
        </div>
      </div>
    </section>


    <!-- ============================================================
         JOURNAL STRIP — Blog (FR-DEST-013..018)
         ============================================================ -->
    <section class="section">
      <div class="container-wide">
        <div class="section-head" data-reveal>
          <div>
            <p class="overline">{{ home.blogHead.overline }}</p>
            <h2>{{ home.blogHead.titleHtml | safe }}</h2>
          </div>
          <a href="/blog" class="btn-text">Read the blog <span class="arrow">→</span></a>
        </div>

        <div class="blog-strip" data-stagger>
{% for p in blogPosts %}
          <a class="blog-card" href="/blog-post?b={{ p.title | slug }}">
            <div class="thumb"><img loading="lazy" src="{{ p.image }}" alt="" srcset="{{ p.image | srcset }}" sizes="(max-width: 640px) 92vw, (max-width: 1024px) 46vw, 30vw" decoding="async"></div>
            <div class="category">{{ p.category }}</div>
            <h3>{{ p.title | safe }}</h3>
            <p class="excerpt">{{ p.excerpt | safe }}</p>
            <div class="byline">{% for b in p.bylineSpans %}{% if not loop.first %}<span class="sep"></span>{% endif %}<span>{{ b }}</span>{% endfor %}</div>
          </a>
{% endfor %}
        </div>
      </div>
    </section>


    {{ bn.zone(banners, 'home-mid') }}

    <!-- ============================================================
         ASK FOR GUIDANCE (FR-HOME-010..011)
         ============================================================ -->
    <section class="section">
      <div class="container-wide">
        {# One centred band, two ways in. The inline form used to sit here;
           /contact now owns every enquiry, and /contact#partner swaps its
           subject dropdown for the partner packages. #}
        <div class="ask-section" data-reveal>
          <p class="overline center">{{ home.ask.overline }}</p>
          <h2>{{ home.ask.titleHtml | safe }}</h2>
          <p class="ask-body">{{ home.ask.body | safe }}</p>
          <div class="actions">
            <a href="/contact" class="btn btn-brand">General enquiry <span class="arrow">→</span></a>
            <a href="/contact#partner" class="btn btn-ghost">Become a partner <span class="arrow">→</span></a>
          </div>
        </div>
      </div>
    </section>

    {% if mediaItems and mediaItems | length %}
    <!-- ============================================================
         MEDIA WALL — FR-HOME-023..027: up to 50 videos + 50 images.
         Click plays a video / opens an image; hover auto-plays a video
         and enlarges an image.
         ============================================================ -->
    <section class="section">
      <div class="container-wide">
        <div class="section-head" data-reveal>
          <div>
            <p class="overline">{{ home.mediaHead.overline if home.mediaHead else 'In Motion' }}</p>
            <h2>{{ (home.mediaHead.titleHtml | safe) if home.mediaHead and home.mediaHead.titleHtml else 'The world, <em>in motion</em>.' }}</h2>
          </div>
          <a href="/gallery" class="btn-text">Full gallery <span class="arrow">→</span></a>
        </div>

        <div class="media-wall" data-stagger>
{% for g in mediaItems %}
          <a data-lightbox{% if g.type == 'video' %} data-type="video"{% endif %} data-src="{{ g.src }}" data-kind="{{ g.type }}"{% if g.type == 'video' and g.src %} data-hover-video{% endif %} class="gallery-tile media-tile{% if g.type == 'video' %} is-video{% endif %}">
            {% if g.type == 'video' and g.src %}
            <video src="{{ g.src }}" poster="{{ g.image }}" muted loop playsinline preload="metadata"></video>
            {% else %}
            <img loading="lazy" src="{{ g.image }}" alt="{{ g.alt }}" srcset="{{ g.image | srcset }}" sizes="(max-width: 640px) 92vw, (max-width: 1024px) 46vw, 30vw" decoding="async">
            {% endif %}
            {% if g.type == 'video' %}<div class="play-icon">▶</div>{% endif %}
            {% if g.label %}<div class="overlay"><span class="tag tag-plain">{{ g.label }}</span></div>{% endif %}
          </a>
{% endfor %}
        </div>
      </div>
    </section>
{% endif %}

    {{ bn.zone(banners, 'home-bottom') }}
  </main>

  <div data-partial="footer"></div>
  <div data-partial="search"></div>
  <div data-partial="consent"></div>
  <div data-partial="lightbox"></div>

  <script src="assets/js/partials.js"></script>
  <script src="assets/js/main.js"></script>
</body>
</html>