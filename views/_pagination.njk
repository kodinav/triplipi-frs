{# Shared page-number navigation — renders nothing when there's only one page #}
{% macro nav(p, baseUrl) %}
  {% if p and p.pages > 1 %}
    <nav class="pagination" aria-label="Pages" data-reveal>
      {% if p.page > 1 %}
        <a class="page-btn arrow" href="{{ baseUrl }}?page={{ p.page - 1 }}" aria-label="Previous page">←</a>
      {% endif %}
      {% for i in range(1, p.pages + 1) %}
        {% if i == p.page %}
          <span class="page-btn is-current" aria-current="page">{{ i }}</span>
        {% else %}
          <a class="page-btn" href="{{ baseUrl }}?page={{ i }}">{{ i }}</a>
        {% endif %}
      {% endfor %}
      {% if p.page < p.pages %}
        <a class="page-btn arrow" href="{{ baseUrl }}?page={{ p.page + 1 }}" aria-label="Next page">→</a>
      {% endif %}
    </nav>
  {% endif %}
{% endmacro %}
